#include <iostream>
#include"header1.h"
using namespace std;

int main()
{
    int n;
    cin>>n;
    //invertedPattern(n);
    // zeronePattern( n);
    //rombusPattern(n);
    //numTriangle(n);
    // palinTriangle(n);
     starPattern(n);


    return 0;
}
